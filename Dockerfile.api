FROM node:24-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV INLET_API_PORT=8787
ENV INLET_DATA_DIR=/data

COPY package*.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY src ./src

EXPOSE 8787

CMD ["npm", "run", "server"]

